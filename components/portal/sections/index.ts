/**
 * THE FOUR PORTAL SECTIONS — and the one place their provenance is recorded.
 *
 * =============================================================================
 * 🔴 PROVENANCE, BECAUSE IT MATTERS MORE HERE THAN ANYWHERE ELSE ON THIS SITE.
 *
 * These four sections follow the STRUCTURE and STEP SEQUENCE of another agency's
 * agent pages, supplied as source material. Not one sentence is theirs. Both
 * agencies recruit from the same pool, so reused phrasing would differentiate
 * neither — the standing Checkmate rule (HANDOFF §2.2) applied to a second
 * competitor.
 *
 * WHAT WAS STRIPPED, AND WHY EACH HAD TO GO:
 *   · Both "download Slack" steps — deleted outright, not swapped for another
 *     chat tool. Synergy has not said what it uses, and inventing one would put
 *     an instruction on the page that leads nowhere.
 *   · Their admin email, phone, Houston address, office hours and named staff.
 *   · Every ffl-oc.com link, their FFL-SRS Gateway bootcamp and their
 *     password-walled bootcamp pages — private systems Synergy cannot reach.
 *   · Their three PDF downloads. Their documents.
 *   · Their "your financial success is our priority" tagline.
 *
 * WHAT WAS KEPT, AND WHY IT IS KEEPABLE — the regulators and providers every
 * licensed agent in the country uses (NIPR, Prepare2Pass, Xcel Solutions,
 * Sircon, WebCE) and the substance of each step, which is industry process
 * rather than anyone's copy: the E&O minimum and its start-date rule, the AML
 * certificate, the contracting document list, the voided-check requirement and
 * the beneficiary detail.
 *
 * =============================================================================
 * 🔴 SEVEN MARKED GAPS, NONE INVENTED. fflsynergy.com was swept on 2026-08-08 —
 * every route in its sitemap, rendered, plus join.fflsynergy.com (a Vercel 404).
 * It publishes ONE contact block and nothing else that fills these:
 *
 *     phone 407-434-0400, Mon–Sat 8am–7pm, info@fflsynergy.com,
 *     5728 Major Blvd Suite 702, Orlando FL 32819
 *
 * The phone and hours are used. `info@fflsynergy.com` is deliberately NOT wired
 * into the document steps: it is published as a CLIENT address ("email
 * info@fflsynergy.com" to book a free consultation), never as where an agent
 * sends a driver's licence and an NPN. Routing onboarding documents into a
 * prospect inbox is a decision for Ziad, not a fact readable off the page.
 *
 * =============================================================================
 * 🔴 COMPLIANCE. No income, earnings or commission figures anywhere, and no
 * volume claims. Contracting step 4 says carriers "set up payment to you
 * directly" — a mechanism, stated only to explain why a voided check is needed,
 * with no amount attached. "A-rated" and "AM Best" appear nowhere near the
 * carrier list (HANDOFF §2.6).
 */
export { default as LicensingSection } from "./LicensingSection";
export { default as ContractingSection } from "./ContractingSection";
export { default as ResourcesSection } from "./ResourcesSection";
export { default as TrainingSection } from "./TrainingSection";
